import React, { FC } from 'react';

const LatexIcon: FC = (props) => {
  return (
    <span style={{fontSize: 20, color: '#1890ff', opacity: 0.7}}>
      &fnof;
    </span>
  )
}

export default LatexIcon;