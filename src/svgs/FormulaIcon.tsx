import React, { FC } from 'react';

const FormulaIcon: FC = (props) => {
  return (
    <span style={{fontSize: 26, color: '#1890ff', opacity: 0.7}}>
      ƒ
    </span>
  )
}

export default FormulaIcon;